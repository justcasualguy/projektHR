package login;

import javax.swing.*;
import java.awt.event.*;
public class asasd extends JDialog{
    private JPanel panel1;
    private JRadioButton radioButton1;
    private JButton button;

    public asasd() {
        setContentPane(panel1);
        setModal(true);
        getRootPane().setDefaultButton(button);
    }
}
